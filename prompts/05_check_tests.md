# Step 5: Check The Tests

Use `CONTEXT.md` and `STATE.json` to identify test coverage targets. Review tests in `tests/` for scope, relevance, and gaps. Ensure critical logic paths are covered (YAML parsing, image compose, launcher behavior, docs checks). Propose new tests if coverage is thin.
