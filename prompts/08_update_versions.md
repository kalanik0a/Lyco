# Step 8: Update Project Versions

Review `pyproject.toml` and `README.md` to confirm versioning is consistent. If a release is planned, update the version and note changes in `CHANGELOG.md` (if present). Ensure changes align with the release policy in `TASKS.md`.
