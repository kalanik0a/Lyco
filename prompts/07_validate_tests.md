# Step 7: Validate The Tests

Validate test results against expected behavior described in `DOCS.md` and `CONTEXT.md`. Ensure tests reflect real usage (CLI, module entry, repo wrapper) and that no test is overly coupled to incidental outputs. If tests pass but coverage is weak, add a coverage note to `TASKS.md`.
